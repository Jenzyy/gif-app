import React from 'react';
import ReactDOM from 'react-dom/client';
import 'animate.css'
import './index.css';
import GitExpertApp from './GiftExpertApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <GitExpertApp />
  
);

